using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/*
 * This script should handle spawning our template prefabs in the world.
 * Generation will maintain a list of blocks within the render ring of the user.
 * This script should interpret that list of grid locations and instantiate the 
 * gameobjects that exist on maptemplate objects.
 * 
 * This is responsible for loading map blocks.
 */
public class TemplateSpawner : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
